# Development and CI dependencies. Runtime dependencies are kept in
# requirements.txt so Railway and local execution use the same application set.
-r requirements.txt
pytest
pytest-cov

# TestClient compatibility: pin the versions verified by the API test suite.
fastapi==0.141.1
starlette==1.3.1
httpx==0.28.1
