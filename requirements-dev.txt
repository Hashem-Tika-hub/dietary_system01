# Development and test tooling for Dietary System.
# Install after the production dependency set when working locally.
-r requirements-deploy.txt
pytest
pytest-cov

# TestClient compatibility: pin the versions verified by the API test suite.
fastapi==0.141.1
starlette==1.3.1
httpx==0.28.1
