# ── أسرار / بيئة ──────────────────────────────────────────
.env
*.env
.env.production
!.env.example
!.env.production.example

# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── قاعدة البيانات المحلية ────────────────────────────────
data/dietary.db
data/*.db
data/*.sqlite3
data/**/*.db
data/**/*.sqlite3
# النسخة المرجعية الثابتة للكتالوج فقط؛ لا تشمل أي بيانات مستخدم.
!data/catalog/
!data/catalog/food_catalog_reference.sqlite3
!data/catalog/food_catalog_reference.manifest.json

# ── Flutter / Dart ────────────────────────────────────────
# هذا اللي كان ناقص فعليًا — build/ سبق وانرفعت بالغلط
# (app/android/build/reports/problems/problems-report.html)
# بينما الكود المصدري نفسه (app/lib/*.dart) اتحذف بنفس الـ commit
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
app/build/
app/android/build/
app/android/.gradle/
app/ios/Pods/
app/ios/.symlinks/
app/*.iml

# ── IDE / نظام التشغيل ────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db