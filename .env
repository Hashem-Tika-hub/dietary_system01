# انسخ هذا الملف إلى .env (بدون .example) واملأ القيم الحقيقية.
# .env نفسه لا يُرفع لـ git أبدًا — مُستبعد فعليًا بـ .gitignore.

# مفتاح USDA FoodData Central — سجّل مفتاح مجاني من:
# https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=yG9SndeWXka7SdQhsADy3DG9UbPuDwEACdFTL24c

# مفتاح توقيع JWT — أنشئ قيمة عشوائية بـ:
#   python -c "import secrets; print(secrets.token_hex(32))"
# لا تستخدم نفس القيمة في أكثر من بيئة (تطوير/إنتاج)، ولو تسربت
# قيمة قديمة يومًا (زي ما صار سابقًا) ولّد وحطّ غيرها فورًا.
SECRET_KEY=10ebba06ede9c71477f39bb38d2073add2c490eda602304fb72bbe0276c70fde

# اختياري — افتراضيًا SQLite محلي، غيّرها فقط لو تستخدم PostgreSQL بالإنتاج
# DATABASE_URL=postgresql://user:password@localhost:5432/dietary_db
