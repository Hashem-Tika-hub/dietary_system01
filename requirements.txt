# Runtime dependencies for local development and Railway deployment.
# Keep this file free of notebook-only packages so the deployed API installs
# only what FastAPI, the recommendation models, and PostgreSQL require.

# API and database
fastapi
uvicorn[standard]
sqlalchemy
alembic
python-jose[cryptography]
bcrypt
passlib
python-multipart
pydantic[email]
slowapi
python-dotenv
psycopg[binary]

# Recommendation models
pandas
numpy
scikit-learn
matplotlib
requests
